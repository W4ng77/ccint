# 已迁移到 config/taxonomy.yaml(T1)。
#
# 这个文件曾是规则 labeler 的 topic 真值源,而 LLM labeler 的 enum 是 llm_v1.py
# 里的另一份 Python 列表。两份可以漂移且不报错 —— 合并时实测到的第一个漂移是
# 两边的标签空间大小不同(10 vs 11)。
#
# 同目录下的 topics.yaml.frozen 是迁移当天的逐字快照,只供测试断言
# 「T1 只搬家不改内容」使用,不参与运行时加载。
migrated_to: ../../../../config/taxonomy.yaml
